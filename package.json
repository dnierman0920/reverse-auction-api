{
  "name": "reverse-auction-api",
  "version": "1.0.0",
  "description": "An Api that tracks projects, bids, vendors and procurers",
  "main": "./src/index.ts",
  "scripts": {
    "start:dev": "npx nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js"
  },
  "keywords": [
    "mongodb",
    "typescript",
    "REST",
    "api"
  ],
  "author": "David Nierman",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.2.5",
    "nodemon": "^2.0.22",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "@types/express": "^4.17.17",
    "dotenv": "^16.1.4",
    "express": "^4.18.2",
    "mongodb": "^5.6.0",
    "ts-dotenv": "^0.9.1"
  }
}
